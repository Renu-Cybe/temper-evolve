# Day 1 — Temper 第一次开口说话

**Time:** 2026-03-28  
**Code:** +50 lines

## What

搭建了最简 agent 骨架：
- 基于阿里云百炼 DashScope API
- 两个工具：read_file / run_bash
- 使用 .env 文件管理 API Key（不提交到 Git）

## Test

Temper 成功调用 `dir` 列出目录内容，识别出所有文件。

## Bug Found

模型一次输出多个工具调用时会解析失败。例如：代码只处理了单个 JSON，多个会抛 `Extra data` 错误。

## Gap

- [x] 支持单次多工具调用
- [x] 无文件写入功能 → 已添加 edit_file
- [ ] 更健壮的 JSON 解析
- [ ] 对话历史保存

---

# Day 1 (续) — 获得「自我修改」能力

**Time:** 2026-03-28 (同天下午)
**Code:** +15 lines

## What

给 Temper 添加了 `edit_file` 工具，让它能够修改文件内容。

```python
def edit_file(path, old_string, new_string):
    try:
        with open(path, 'r', encoding='utf-8') as f:
            content = f.read()
        if old_string not in content:
            return f"Error: 在 {path} 中未找到匹配内容"
        content = content.replace(old_string, new_string, 1)
        with open(path, 'w', encoding='utf-8') as f:
            f.write(content)
        return f"成功修改 {path}"
    except Exception as e:
        return f"Error: {e}"
```

## Why

这是 Temper 进化的关键一步：能读 → 能分析 → **能修复**

现在可以用自然语言驱动自修复：
```
读取 temper.py，然后修复第78行的 bug
```

## Next

**目标：首次自修复**

启动 Temper，输入：
```
读取 temper.py，分析为什么一次只能处理一个工具调用，然后修复它。
```

---

*Temper can now heal itself.*